# Directories to scan for file context (one per line)
# These are injected into prompts for non-Claude models so they can "see" your files
# Supports ~ and $HOME expansion
# Only file names + sizes are listed (not contents) to keep prompts small
~/Documents
~/Documents/Grid/knowledge
